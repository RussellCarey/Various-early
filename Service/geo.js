/* async function getLocation() {
  try {
    const geo = await fetch("https://api.ipregistry.co/?key=uwz7nfd8n114ny"); // Gets the data
    const data = await geo.json(); // Puts data to a json (promise);
    console.log(data.location.language.name);
  } catch (err) {
    alert(err); // Failed to fetch
  }
}

getLocation();
*/
